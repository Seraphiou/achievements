import reducer from '../../src/reducers';
import { expect } from 'chai';

describe('Reducer', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('should fail', () => {
    expect(true).to.be.equal(true);
  })
});